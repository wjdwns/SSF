<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ssf">

    <!-- 인터넷 퍼미션 (권한) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".Activity.Search_SettingActivity"></activity>
        <activity android:name=".Activity.CommentActivity"></activity>
        <activity android:name=".Activity.Keyword_searchActivity" />
        <activity android:name=".Activity.loginActivity" />
        <activity android:name=".Activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.MainActivity"></activity>
        <activity android:name=".Activity.DetailPageActivity" ></activity>
        <activity android:name=".List.SearchList">
        </activity>
    </application>

</manifest>